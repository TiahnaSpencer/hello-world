#include <iostream>
#include <vector>
#include <string>
#include <fstream>
using namespace std;

vector<string>attendance = {"Envy ", "Ilyas ", "Damon ", "Hamir " ,"Ajay ", "Bode ", "George ", "Jordan ", "Chris ", "Caleb ", "Seth ", "Ryan ", "Tiahna ", "Bailin ", "Elijah ", "Connor "};

bool isValidInput(string input) {
  if (input == "0" || input == "1") {
    return true;
  }
  return false;
}

int main() {
  string answer;
  string date;
 
  cout << "Please enter todays date (MM/DD/YY): ";
  cin >> date;

  ofstream MyFile(date + ".txt");

  for (int i = 0; i < attendance.size(); i++) {
    cout << "Is " << attendance[i] << "here? (1 for yes, 0 for no): ";
    cin >> answer;
    
    while (!isValidInput(answer)) {
      cout << "Invalid input. Please enter 1 or 0: ";
      cin >> answer;
    }
    
    if (answer == "1") {
      MyFile << attendance[i] << " is present.\n\n";
      cout << attendance[i] << " is present.\n\n";
    } else if (answer == "0") {
      MyFile << attendance[i] << " is absent.\n\n";
      cout << attendance[i] << " is absent.\n\n";
    }
  }
  
  MyFile.close();
  return 0;
}
